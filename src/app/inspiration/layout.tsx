export const metadata = {
  title: "Generate Inspiration",
  description: "Generate your own inspiration!",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
